import { FormataDecimalDirective } from './formata-decimal.directive';

describe('FormataDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new FormataDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
